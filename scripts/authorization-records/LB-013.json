{
  "schema_version": 1,
  "records": [
    {
      "authorization_id": "EXEC-PREAUTH-LB013-001",
      "scope": [
        "src-tauri/tauri.conf.json",
        "scripts/authorization-records/LB-013.json"
      ],
      "actions": [
        "Remove the static main-window declaration from tauri.conf.json",
        "Require foreground startup and tray-open to create the main window dynamically in Rust",
        "Ensure --background reaches Tauri setup without creating the main WebView window"
      ],
      "reason": "LB-013 requires --background to create no visible/main window from process entry; the inherited static tauri.conf.json window would be created before Rust could hide it, which violates the frozen background lifecycle contract.",
      "evidence_ref": "Standing user authorization permits concrete executor pre-authorizations with later audit; LB-013 contract requires '--background no visible window' and docs/02_ARCHITECTURE_RUNTIME.md requires entry-time background mode rather than create-then-hide.",
      "recorded_by": "LocalBridge Development Agent",
      "user_audit_status": "PENDING",
      "does_not_expand_future_pr_writable_paths": true
    },
    {
      "authorization_id": "EXEC-PREAUTH-LB013-002",
      "scope": [
        "src-tauri/src/runtime/orchestrator.rs",
        "scripts/authorization-records/LB-013.json"
      ],
      "actions": [
        "Add staged shutdown primitives that stop Tunnel first and retain PEP/MCP ownership",
        "Allow the desktop exit coordinator to close the privileged call gate and stop Broker between Tunnel shutdown and PEP/MCP shutdown",
        "Keep RuntimeOrchestrator::stop() externally compatible by composing the same staged shutdown primitives"
      ],
      "reason": "The frozen privileged lifecycle requires Tray Exit ordering Tunnel -> privileged request gate closed -> Broker -> PEP -> MCP. The existing monolithic RuntimeOrchestrator::stop() performs Tunnel -> PEP -> MCP without an insertion point for Broker shutdown, so the production lifecycle could not satisfy the documented security ordering.",
      "evidence_ref": "Standing user authorization permits concrete executor pre-authorizations with later audit; docs/12_PRIVILEGED_BROKER.md freezes Tray Exit as Tunnel then privileged gate/Broker then PEP then MCP, while current RuntimeOrchestrator::stop() is monolithic.",
      "recorded_by": "LocalBridge Development Agent",
      "user_audit_status": "PENDING",
      "does_not_expand_future_pr_writable_paths": true
    },
    {
      "authorization_id": "EXEC-PREAUTH-LB013-003",
      "scope": [
        "src-tauri/src/runtime/orchestrator.rs",
        "scripts/authorization-records/LB-013.json"
      ],
      "actions": [
        "Add an owned CredentialStore path to ProductionRuntimeDriver while preserving the existing borrowed constructor for prior G2 tests",
        "Add only a cfg(test) ready-runtime constructor needed to exercise shutdown against actual production MCP/PEP/Tunnel adapter handles"
      ],
      "reason": "G3 adversarial review commit fc9f4eb found that DesktopLifecycle could not own the borrowed ProductionRuntimeDriver as a long-lived production runtime, leaving Tray Exit with runtime=None even when the frozen lifecycle requires Tunnel -> privileged gate/Broker -> PEP -> MCP cleanup.",
      "evidence_ref": "Review fc9f4eb and current source show DesktopLifecycle::register_runtime had no production caller and ProductionRuntimeDriver borrowed CredentialStore; this preauthorization is limited to making that adapter ownable and testable without broadening LB-013 writable paths.",
      "recorded_by": "LocalBridge Development Agent",
      "user_audit_status": "PENDING",
      "does_not_expand_future_pr_writable_paths": true
    },
    {
      "authorization_id": "EXEC-PREAUTH-LB013-004",
      "scope": [
        "src-tauri/src/tray/mod.rs",
        "tests/integration/background/lb013_contract.test.mjs",
        "scripts/authorization-records/LB-013.json"
      ],
      "actions": [
        "Render the frozen LocalBridge default window icon in the Windows tray through a tray-only 90 percent center crop so the notification-area glyph is visually larger",
        "Keep assets/icons/localbridge.png and assets/icons/localbridge.ico byte-for-byte unchanged and preserve their frozen hashes for application and installer branding",
        "Add regression coverage proving the tray still originates from the frozen default window icon and the visual zoom cannot silently disappear"
      ],
      "reason": "Human manual review found the Windows notification-area icon visually undersized. The user explicitly authorized this LB-013 cross-PR correction on 2026-08-13; a tray-only in-memory crop is the narrowest change because it improves small-slot legibility without replacing or redrawing the frozen brand asset.",
      "evidence_ref": "Explicit user authorization on 2026-08-13: '授权，和修改', referring to the reported LB-013 tray icon visual-size issue.",
      "recorded_by": "LocalBridge Development Agent",
      "user_audit_status": "PENDING",
      "does_not_expand_future_pr_writable_paths": true
    },
    {
      "authorization_id": "EXEC-PREAUTH-LB013-005",
      "scope": [
        "src-tauri/src/tray/mod.rs",
        "src-tauri/src/main.rs",
        "tests/integration/background/lb013_contract.test.mjs",
        "tests/e2e/dashboard/lb015_window_chrome_contract.test.mjs",
        "tests/e2e/onboarding/lb016_contract.test.mjs",
        "tests/e2e/onboarding/fixed_window_runtime_e2e.mjs",
        "PR_INDEX.json",
        "PROJECT_STATE.json",
        "scripts/authorization-records/LB-013.json"
      ],
      "actions": [
        "Roll back the physical-pixel 900x620 window lock and inverse WebView zoom introduced by the earlier DPI correction, restoring fixed 900x620 logical/DIP inner-size semantics so Windows and WebView2 perform normal DPI scaling",
        "Update only the directly dependent LB-013/LB-015/LB-016 regression and live fixed-window E2E assertions so physical size divided by native scale resolves to 900x620 logical while fixed borderless custom-chrome behavior remains unchanged",
        "Preserve the frozen localbridge.ico tray implementation that selects a native DPI-matched ICO frame without resampling; do not restore the old crop/default-window-icon path"
      ],
      "reason": "The user explicitly requested rollback of the prior Windows DPI sizing semantic correction after it had been described as forcing a physical 900x620 native client at 150% scaling. The physical lock plus inverse WebView zoom is separable from the later native-DPI tray icon fix and can be removed without changing the frozen product fixed-window contract.",
      "evidence_ref": "Explicit user instruction on 2026-08-13: '回调：修正 Windows DPI 尺寸语义。实机 150% 缩放下原生 client 已验证为物理 900×620'",
      "recorded_by": "LocalBridge Development Agent",
      "user_audit_status": "PENDING",
      "does_not_expand_future_pr_writable_paths": true
    }
  ]
}
